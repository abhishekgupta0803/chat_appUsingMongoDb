<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <p><b><%= chat._id %></b></p>
    <p><b>Message From: <%= chat.from %> and  Send To: <%= chat.to %></b></p>

    <form  method="post" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" rows="13px"  cols="50px"> <%= chat.msg %></textarea>
        <br>
        <button>Edit</button>
    </form>
</body>
</html>